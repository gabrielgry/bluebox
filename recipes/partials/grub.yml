---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: files
    files:
      - source: system/grub
        destination: /

  - type: script
    snippets:
      - systemctl enable grub-hide-config.service